apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inferqueue-ingress
  namespace: inferqueue
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - inferqueue.example.com
      secretName: inferqueue-tls
  rules:
    - host: inferqueue.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 80